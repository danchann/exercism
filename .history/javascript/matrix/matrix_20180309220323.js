var Matrix = {
arr = str.split('\n'),

columns: function (str) {

},

rows: function (str) {

}






for (var x in arr) {
    mx.push(arr[x].split(' '))
}

for (y=0; y<mx.length; y++) {
    result.push(mx[y][1])
}



module.exports = Matrix;


