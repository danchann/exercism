function Matrix (input) {

    this.columns = [];
    this.rows = [];

arr = str.split('\n');

for (var x in arr) {
    rows.push(arr[x].split(' '))
}

// get column
for (y=0; y<mx.length; y++) {
    result.push(mx[y][z])
}

}





module.exports = Matrix;


