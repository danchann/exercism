function Matrix (input) {

    this.columns = [];
    this.rows = [];

    arr = str.split('\n');

    for (var x in arr) {
        this.rows.push(arr[x].split(' '))
    }

    // get column
    for (y=0; y<this.rows.length; y++) {
        this.columns[y]
        result.push(mx[y][z])
    }

    }

module.exports = Matrix;


